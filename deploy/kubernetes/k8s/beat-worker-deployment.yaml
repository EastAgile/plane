apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -f docker-compose.yaml -o k8s/
    kompose.version: 1.34.0 (cbf2835db)
  labels:
    io.kompose.service: beat-worker
  name: beat-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: beat-worker
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -f docker-compose.yaml -o k8s/
        kompose.version: 1.34.0 (cbf2835db)
      labels:
        io.kompose.service: beat-worker
    spec:
      imagePullSecrets:
        - name: docker-registry-key
      containers:
        - args:
            - ./bin/docker-entrypoint-beat.sh
          envFrom:
            - secretRef:
                name: plane-secrets
            - configMapRef:
                name: plane-config
          image: rememberizer/plane-backend:local
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          name: beat-worker
          volumeMounts:
            - mountPath: /code/plane/logs
              name: logs-beat-worker
      restartPolicy: Always
      volumes:
        - name: logs-beat-worker
          persistentVolumeClaim:
            claimName: logs-beat-worker
